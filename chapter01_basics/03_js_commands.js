const os = require('os');
//console.log("OS Type (os.type):", os.type());
//console.log("OS Platform (os.platform):", os.platform());
//console.log("OS Release:", os.release());

console.log("OS Platform (process.platform):", process.platform);
console.log("OS Arch (os.arch):", os.arch());


